[project]
name = "predictive-maintenance-modeling-time-to-failure-us"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "lifelines>=0.30.3",
    "matplotlib>=3.10.9",
    "numpy>=2.4.4",
    "pandas>=2.3.3",
    "pytest>=9.0.3",
    "pyyaml>=6.0.3",
]
